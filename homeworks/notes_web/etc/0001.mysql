BEGIN;

CREATE TABLE notes (
    id BIGINT NOT NULL PRIMARY KEY,
    create_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    user VARCHAR(30) NOT NULL,
    note VARCHAR(255) NOT NULL,
    title VARCHAR(30) NOT NULL DEFAULT NOW()
);

COMMIT;